1. Tu base de datos principal debe estar en la nube.
2. Usaremos orm para hacer de puente(api) y se conectara a la bd
    App Móvil (React Native) a Petición HTTP (fetch/axios) a API (en la nube) a Base de Datos (en la nube).
3. Usaremos supabase
4. Hacer todas las pantallas sin datos reales, solo funcionamiento y diseño base